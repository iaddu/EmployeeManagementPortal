spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ems?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl_auto: update

